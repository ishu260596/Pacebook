<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ishwar_arcore.pacebook">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_sixbig"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_sixbig"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pacebook">
        <activity android:name=".views.addposts.ChoosePostActivity"></activity>
        <activity android:name=".profile.ProfileActivity" />
        <activity android:name=".views.launcher.LauncherActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.mainactivity.MainActivity" />
        <activity android:name=".views.loginandsignup.ForgetPasswordActivity" />
        <activity android:name=".views.loginandsignup.RegisterActivity" />
        <activity android:name=".views.loginandsignup.LoginActivity" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>